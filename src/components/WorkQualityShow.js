import React from 'react';

const WorkQualityShow = (props) => {
    return (
        <div>
            work quality show.
            {props.data}
        </div>
    );
};

export default WorkQualityShow;
